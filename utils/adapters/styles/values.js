export const toPX = (val) => val && `${val}px`;
export const toPerc = (val) => val && `${val}%`;
export const toEm = (val) => val && `${val}em`;
export const toVw = (val) => val && `${val}vw`;
export const toVh = (val) => val && `${val}vh`;